import React from 'react';
import SearchInput from '../components/SearchInput';

export default {
  title: 'Search',
  component: SearchInput,
};

export const Search = () => <SearchInput placeholder={'Search ingredients'} />;
